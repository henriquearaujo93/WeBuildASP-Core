@model IEnumerable<WeBuildASP.Models.Team>


@{
    ViewData["Title"] = "Teams";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-default">Create New</a> <a asp-action="GoAdminMenu" class="btn btn-default">Home</a>
</p>

<table class="table table-striped table-hover">
    <thead>
        <tr class="success">
            <th>
                @Html.DisplayNameFor(model => model.T_COUNTRY)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_INCHAR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_ACTIVE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.T_COUNTRY)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.T_INCHAR)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.T_ACTIVE)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.ID">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>
